name: on_push

on:
  push:
    branches-ignore:
      - 'main'

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: yarn prettier
      - run: git diff --exit-code
